// API test

describe('Test /GET launches', () => { 
    test('it should return status 200', () => {
        const response = 200;
        expect(response).toBe(200);
    });
 })
describe('Test /POST launches', () => { 
    test('it should be a function', () => {});
    test('there doest exist missed prop', () => {});
})